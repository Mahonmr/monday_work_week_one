<%= form_for(@species) do |f| %>
  <% if @species.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@species.errors.count, "error") %> prohibited this species from being saved:</h2>

      <ul>
      <% @species.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <label for="inputEmail3" class="col-sm-2 form-control-label"><%= f.label :name %></label>
    <div class="col-sm-10">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="form-group row">
    <label class="col-sm-2">Endangered Species</label>
    <div class="col-sm-10">
      <div class="radio">
        <label><%= radio_button 'species', 'endangered_species', 'yes', :checked => true %>Yes</label>
      </div>
      <div class="radio">
        <label><%= radio_button 'species', 'endangered_species', 'no' %>No</label>
      </div>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
